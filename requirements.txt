# AI Speech Assistant - Render Deployment
# Only direct project dependencies. Pip resolves transitive deps automatically.

# Django core
Django==6.0.6
django-crispy-forms==2.6
crispy-bootstrap5==2026.3
whitenoise==6.12.0
psycopg2-binary==2.9.12
gunicorn==26.0.0

# Environment
python-dotenv==1.2.2

# Speech Recognition
openai-whisper==20250625
SpeechRecognition==3.11.0
pydub==0.25.1

# Translation
deep-translator==1.11.4

# Text to Speech
gTTS==2.5.4

# Document generation
reportlab==5.0.0
python-docx==1.2.0

# Torch (CPU-only for Render — much smaller than CUDA build)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
